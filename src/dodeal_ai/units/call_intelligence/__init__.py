"""Unit B -- call intelligence: a recorded sales call in, a transcript and
its analysis out, asynchronously, through a job and a signed callback.

Everything here is off per tenant until its `unit_b` section says otherwise
(config.py). No real speech-to-text adapter exists yet.
"""
